# ============================================================
# VARIABLES DE ENTORNO — FRONTEND (Vue)
# ============================================================
# Copia este archivo como .env.local y completa los valores
# IMPORTANTE: Solo las variables con prefijo VITE_ son
# accesibles desde el código del frontend.

# URL del backend (Next.js API)
VITE_API_URL=http://localhost:3000/api
